import type {Request,RequestHandler} from 'express'; import {readSession} from '../auth/cookies.js'; import {resolve} from '../services/session-service.js'; import {AppError} from '../errors/app-error.js'; export const requireAuth:RequestHandler=async(req,res,next)=>{try{const id=await resolve(readSession(req.headers.cookie));if(!id)throw new AppError(401,'Authentication required','UNAUTHENTICATED'); (req as Request & {userId?:string}).userId=id; next();}catch(e){next(e)}};