import {api} from '../lib/api'; import type {AuthResponse,User} from '../types/auth'; export const login=(email:string,password:string)=>api<AuthResponse>('/auth/login',{method:'POST',body:JSON.stringify({email,password})}); export const register=(email:string,password:string)=>api<AuthResponse>('/auth/register',{method:'POST',body:JSON.stringify({email,password})}); export const logout=()=>api<void>('/auth/logout',{method:'POST'}); export const me=()=>api<{user:User}>('/auth/me');